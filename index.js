

class Order{
    arrays(arraytoOrder, typeOrder = 'ASC')
    {
        try{            
          switch(typeOrder)      
          {            
          }
          for(let iterator = 0; iterator <= arraytoOrder.length; iterator++)      
          {
            
          }        
        }catch(warnMessage)
        {
            console.warn(warnMessage);
        }
    }
}

module.exports = Order;